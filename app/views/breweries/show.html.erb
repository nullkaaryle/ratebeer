<% cache @brewery do %> 

  <h2> <%= @brewery.name %> 
    <% if not @brewery.active  %>
      <span class="badge badge-secondary"><i>retired</i></span>
    <% end %>
  </h2>
  
  <p>
    <i>Established at  <%= @brewery.year %> </i>
  </p>

  <p>
    <%= link_to "change activity", toggle_activity_brewery_path(@brewery.id), method: :post, class: "btn btn-success btn-sm" %>
  </p>

  <br>

  <p>
    <strong>
      Number of beers: <%= @brewery.beers.count %>
    </strong>
  </p>

  <p>
    <% @brewery.beers.each do |beer| %>
      <%= link_to beer.name, beer %>
    <% end %>
  </p>

  <% if  @brewery.ratings.count > 0 %>
    <p> 
      <strong>
        Has <%= pluralize(@brewery.ratings.count, 'rating') %>, average <%= @brewery.average_rating.round(1) %>
      </strong>
    </p>  
  <% end %>

<% end %>

<br>

<%= edit_and_destroy_buttons(@brewery) %>